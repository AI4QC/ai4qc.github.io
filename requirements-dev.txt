# Linter toolchain. Keep these pinned and bump them deliberately -- the previous
# 2020-era pins (black 19.10b0 / pylint 2.4.4 / mypy 0.761 / isort 4) broke when
# click 8.1 removed the private API black 19.10b0 imported.
black==25.1.0
isort==6.0.1
pylint==3.3.4
mypy==1.15.0
